/** @type {import('next').NextConfig} */
const nextConfig = {
    // redirects: async () => {
    //     return [
    //         {
    //             source: '/',
    //             destination: '/sign-in',
    //             permanent: false, // Optional: Make it a permanent redirect (308)
    //         },
    //         // Add more redirects as needed
    //     ];
    // },
}
export default nextConfig;
